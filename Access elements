#include <stdio.h>

int main() {
    int arr[] = {5, 10, 15, 20, 25};
    int *ptr;

    ptr = arr; // pointer points to first element

    for(int i = 0; i < 5; i++) {
        printf("%d ", *(ptr + i)); // accessing using pointer
    }

    return 0;
}
