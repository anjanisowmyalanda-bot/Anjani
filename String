#include <stdio.h>
#include <string.h>

int main()
{
    char a[20] = "Hello";
    char b[20] = "world";

    printf("Length of a string a = %lu\n", strlen(a));

    strcat(a, b);
    printf("Concatenated string = %s\n", a);

    printf("Comparison result = %d\n", strcmp(a, b));

    return 0;
}
